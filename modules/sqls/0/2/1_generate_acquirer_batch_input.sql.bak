SET SERVEROUTPUT ON;
/
BEGIN
	 T3MP_CREATE_BATCH_INPUT(
	     p_institution_number => '&institution_number',
	     p_client_number => '&client_number',
	     p_card_number => '&card_number__5154968300000008',
	     p_currency => '&currency__979',
		 p_audit_trail => '&audit_trail__MANUAL',
		 p_run_soa_process => '&run_soa_process__N_Y'
	 	);
END;
/
select * from INT_BATCH_CAPTURE where institution_number = '&institution_number' and audit_trail = '&audit_trail_MANUAL';
